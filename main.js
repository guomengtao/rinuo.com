// main.js - 仅作为模块导入清单，不包含业务逻辑
import '/assets/js/bookmarks.js';       // 导入即触发自启动
import '/assets/js/email-subscription.js'; // 导入即触发自启动

// 如需自定义配置，可单独调用（可选）
// import { initEmailSubscription } from './features/emailSubscription.js';
// initEmailSubscription('customContainer', { title: '自定义订阅' });
    